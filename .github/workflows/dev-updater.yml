name: Updater Deploy (Development)

on:
  push:
    branches-ignore:
      - "main"
      - "release/**"
    paths:
      - "apps/updater/**"

permissions:
  id-token: write
  contents: read
  packages: read

jobs:
  test:
    uses: ./.github/workflows/test.yml

  run-migrations:
    uses: ./.github/workflows/run-migrations.yml
    with:
      environment: Development
    secrets: inherit

  deploy-updater:
    needs: [run-migrations, test]
    uses: ./.github/workflows/deploy-updater.yml
    with:
      environment: Development
      sst-stage: dev
    secrets: inherit