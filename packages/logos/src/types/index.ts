export interface CarLogo {
  brand: string;
  url: string;
  filename: string;
}

export interface LogoMetadata {
  brand: string;
  filename: string;
  url: string;
  createdAt: string;
  fileSize?: number;
}
