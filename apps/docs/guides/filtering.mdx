---
title: 'Filtering Guide'
description: 'Master advanced filtering techniques to get exactly the data you need'
---

## Overview

The SG Cars Trends API provides powerful filtering capabilities to help you extract specific data subsets. This guide covers all available filtering options and best practices.

## Basic Filtering

### Month Filtering

Filter data by specific months using the `month` parameter in `YYYY-MM` format:

<CodeGroup>

```bash curl
curl "https://api.sgcarstrends.com/v1/cars?month=2024-01" \
  -H "Authorization: Bearer YOUR_API_KEY"
```

```javascript JavaScript
const response = await fetch('https://api.sgcarstrends.com/v1/cars?month=2024-01', {
  headers: { 'Authorization': 'Bearer YOUR_API_KEY' }
});
```

```python Python
import requests

response = requests.get(
    'https://api.sgcarstrends.com/v1/cars',
    params={'month': '2024-01'},
    headers={'Authorization': 'Bearer YOUR_API_KEY'}
)
```

</CodeGroup>

### Make Filtering

Filter by specific car manufacturers:

<CodeGroup>

```bash curl
curl "https://api.sgcarstrends.com/v1/cars?make=Toyota" \
  -H "Authorization: Bearer YOUR_API_KEY"
```

```javascript JavaScript
const response = await fetch('https://api.sgcarstrends.com/v1/cars?make=Toyota', {
  headers: { 'Authorization': 'Bearer YOUR_API_KEY' }
});
```

```python Python
response = requests.get(
    'https://api.sgcarstrends.com/v1/cars',
    params={'make': 'Toyota'},
    headers={'Authorization': 'Bearer YOUR_API_KEY'}
)
```

</CodeGroup>

## Advanced Filtering

### Multiple Parameters

Combine multiple filters for precise data extraction:

<CodeGroup>

```bash curl
curl "https://api.sgcarstrends.com/v1/cars?month=2024-01&make=Toyota&fuel_type=Hybrid" \
  -H "Authorization: Bearer YOUR_API_KEY"
```

```javascript JavaScript
const params = new URLSearchParams({
  month: '2024-01',
  make: 'Toyota',
  fuel_type: 'Hybrid'
});

const response = await fetch(`https://api.sgcarstrends.com/v1/cars?${params}`, {
  headers: { 'Authorization': 'Bearer YOUR_API_KEY' }
});
```

```python Python
params = {
    'month': '2024-01',
    'make': 'Toyota',
    'fuel_type': 'Hybrid'
}

response = requests.get(
    'https://api.sgcarstrends.com/v1/cars',
    params=params,
    headers={'Authorization': 'Bearer YOUR_API_KEY'}
)
```

</CodeGroup>

### Date Range Filtering

Use `from` and `to` parameters for date range queries (available on COE endpoints):

<CodeGroup>

```bash curl
curl "https://api.sgcarstrends.com/v1/coe?from=2024-01&to=2024-06" \
  -H "Authorization: Bearer YOUR_API_KEY"
```

```javascript JavaScript
const params = new URLSearchParams({
  from: '2024-01',
  to: '2024-06'
});

const response = await fetch(`https://api.sgcarstrends.com/v1/coe?${params}`, {
  headers: { 'Authorization': 'Bearer YOUR_API_KEY' }
});
```

```python Python
params = {
    'from': '2024-01',
    'to': '2024-06'
}

response = requests.get(
    'https://api.sgcarstrends.com/v1/coe',
    params=params,
    headers={'Authorization': 'Bearer YOUR_API_KEY'}
)
```

</CodeGroup>

## Fuel Type Filtering

### Available Fuel Types

<CardGroup cols={2}>
  <Card title="Petrol">
    Traditional gasoline vehicles
  </Card>
  <Card title="Diesel">
    Diesel-powered vehicles
  </Card>
  <Card title="Electric">
    Battery electric vehicles (BEV)
  </Card>
  <Card title="Hybrid">
    Petrol-electric hybrid vehicles
  </Card>
  <Card title="Others">
    Alternative fuel types
  </Card>
</CardGroup>

### Filter by Fuel Type

<CodeGroup>

```bash curl
curl "https://api.sgcarstrends.com/v1/cars?fuel_type=Electric" \
  -H "Authorization: Bearer YOUR_API_KEY"
```

```javascript JavaScript
const response = await fetch('https://api.sgcarstrends.com/v1/cars?fuel_type=Electric', {
  headers: { 'Authorization': 'Bearer YOUR_API_KEY' }
});
```

```python Python
response = requests.get(
    'https://api.sgcarstrends.com/v1/cars',
    params={'fuel_type': 'Electric'},
    headers={'Authorization': 'Bearer YOUR_API_KEY'}
)
```

</CodeGroup>

### Dedicated Fuel Type Endpoints

Use specific endpoints for fuel type data:

<CodeGroup>

```bash curl
# Get all electric vehicle data
curl "https://api.sgcarstrends.com/v1/cars/fuel-types/Electric" \
  -H "Authorization: Bearer YOUR_API_KEY"

# Get electric vehicle data for specific month
curl "https://api.sgcarstrends.com/v1/cars/fuel-types/Electric?month=2024-01" \
  -H "Authorization: Bearer YOUR_API_KEY"
```

```javascript JavaScript
// Get all electric vehicle data
const response = await fetch('https://api.sgcarstrends.com/v1/cars/fuel-types/Electric', {
  headers: { 'Authorization': 'Bearer YOUR_API_KEY' }
});

// Get electric vehicle data for specific month
const monthlyResponse = await fetch('https://api.sgcarstrends.com/v1/cars/fuel-types/Electric?month=2024-01', {
  headers: { 'Authorization': 'Bearer YOUR_API_KEY' }
});
```

</CodeGroup>

## Vehicle Type Filtering

### Available Vehicle Types

<CardGroup cols={2}>
  <Card title="Passenger Cars">
    Standard passenger vehicles
  </Card>
  <Card title="Goods & Other Vehicles">
    Commercial and utility vehicles
  </Card>
  <Card title="Motorcycles & Scooters">
    Two-wheeled vehicles
  </Card>
  <Card title="Buses">
    Public and private buses
  </Card>
  <Card title="Taxis">
    Licensed taxi vehicles
  </Card>
  <Card title="Rental Cars">
    Car rental fleet vehicles
  </Card>
</CardGroup>

### Filter by Vehicle Type

<CodeGroup>

```bash curl
curl "https://api.sgcarstrends.com/v1/cars?vehicle_type=Passenger Cars" \
  -H "Authorization: Bearer YOUR_API_KEY"
```

```javascript JavaScript
const response = await fetch('https://api.sgcarstrends.com/v1/cars?vehicle_type=Passenger Cars', {
  headers: { 'Authorization': 'Bearer YOUR_API_KEY' }
});
```

```python Python
response = requests.get(
    'https://api.sgcarstrends.com/v1/cars',
    params={'vehicle_type': 'Passenger Cars'},
    headers={'Authorization': 'Bearer YOUR_API_KEY'}
)
```

</CodeGroup>

### Dedicated Vehicle Type Endpoints

<CodeGroup>

```bash curl
# Get all passenger car data
curl "https://api.sgcarstrends.com/v1/cars/vehicle-types/Passenger Cars" \
  -H "Authorization: Bearer YOUR_API_KEY"

# URL encode spaces in vehicle type
curl "https://api.sgcarstrends.com/v1/cars/vehicle-types/Passenger%20Cars?month=2024-01" \
  -H "Authorization: Bearer YOUR_API_KEY"
```

```javascript JavaScript
// Get all passenger car data
const response = await fetch('https://api.sgcarstrends.com/v1/cars/vehicle-types/Passenger Cars', {
  headers: { 'Authorization': 'Bearer YOUR_API_KEY' }
});

// URL encode spaces automatically
const encodedType = encodeURIComponent('Passenger Cars');
const monthlyResponse = await fetch(`https://api.sgcarstrends.com/v1/cars/vehicle-types/${encodedType}?month=2024-01`, {
  headers: { 'Authorization': 'Bearer YOUR_API_KEY' }
});
```

</CodeGroup>

## Make-Specific Filtering

### Filter by Manufacturer

<CodeGroup>

```bash curl
curl "https://api.sgcarstrends.com/v1/cars/makes/Toyota" \
  -H "Authorization: Bearer YOUR_API_KEY"
```

```javascript JavaScript
const response = await fetch('https://api.sgcarstrends.com/v1/cars/makes/Toyota', {
  headers: { 'Authorization': 'Bearer YOUR_API_KEY' }
});
```

```python Python
response = requests.get(
    'https://api.sgcarstrends.com/v1/cars/makes/Toyota',
    headers={'Authorization': 'Bearer YOUR_API_KEY'}
)
```

</CodeGroup>

### Get Available Makes

First, get the list of available manufacturers:

<CodeGroup>

```bash curl
curl "https://api.sgcarstrends.com/v1/cars/makes" \
  -H "Authorization: Bearer YOUR_API_KEY"
```

```javascript JavaScript
const response = await fetch('https://api.sgcarstrends.com/v1/cars/makes', {
  headers: { 'Authorization': 'Bearer YOUR_API_KEY' }
});
const makes = await response.json();
console.log(makes.data); // Array of available makes
```

</CodeGroup>

## Pagination and Limits

### Basic Pagination

<CodeGroup>

```bash curl
# Get first page (default)
curl "https://api.sgcarstrends.com/v1/cars?page=1&limit=20" \
  -H "Authorization: Bearer YOUR_API_KEY"

# Get second page
curl "https://api.sgcarstrends.com/v1/cars?page=2&limit=20" \
  -H "Authorization: Bearer YOUR_API_KEY"
```

```javascript JavaScript
// Function to get paginated results
async function getPaginatedData(page = 1, limit = 20) {
  const response = await fetch(`https://api.sgcarstrends.com/v1/cars?page=${page}&limit=${limit}`, {
    headers: { 'Authorization': 'Bearer YOUR_API_KEY' }
  });
  return await response.json();
}

// Get first page
const firstPage = await getPaginatedData(1, 20);
console.log(`Total records: ${firstPage.total}`);
console.log(`Page 1 of ${Math.ceil(firstPage.total / 20)}`);
```

</CodeGroup>

### Pagination with Filters

<CodeGroup>

```javascript JavaScript
// Paginate through filtered results
async function getPaginatedFilteredData(filters, page = 1, limit = 20) {
  const params = new URLSearchParams({
    ...filters,
    page: page.toString(),
    limit: limit.toString()
  });
  
  const response = await fetch(`https://api.sgcarstrends.com/v1/cars?${params}`, {
    headers: { 'Authorization': 'Bearer YOUR_API_KEY' }
  });
  
  return await response.json();
}

// Example usage
const filters = {
  month: '2024-01',
  fuel_type: 'Electric'
};

const results = await getPaginatedFilteredData(filters, 1, 10);
```

</CodeGroup>

## Common Filtering Patterns

### 1. Monthly Analysis

Get comprehensive data for a specific month:

<CodeGroup>

```javascript JavaScript
async function getMonthlyAnalysis(month) {
  const [cars, coe] = await Promise.all([
    fetch(`https://api.sgcarstrends.com/v1/cars?month=${month}`, {
      headers: { 'Authorization': 'Bearer YOUR_API_KEY' }
    }).then(r => r.json()),
    fetch(`https://api.sgcarstrends.com/v1/coe?month=${month}`, {
      headers: { 'Authorization': 'Bearer YOUR_API_KEY' }
    }).then(r => r.json())
  ]);
  
  return { cars: cars.data, coe: coe.data };
}

const analysis = await getMonthlyAnalysis('2024-01');
```

</CodeGroup>

### 2. Electric Vehicle Trends

Track electric vehicle adoption over time:

<CodeGroup>

```javascript JavaScript
async function getElectricVehicleTrends() {
  const months = ['2024-01', '2024-02', '2024-03', '2024-04', '2024-05', '2024-06'];
  
  const trends = await Promise.all(
    months.map(async (month) => {
      const response = await fetch(`https://api.sgcarstrends.com/v1/cars/fuel-types/Electric?month=${month}`, {
        headers: { 'Authorization': 'Bearer YOUR_API_KEY' }
      });
      const data = await response.json();
      return {
        month,
        total: data.data.reduce((sum, item) => sum + item.number, 0)
      };
    })
  );
  
  return trends;
}

const evTrends = await getElectricVehicleTrends();
```

</CodeGroup>

### 3. Manufacturer Comparison

Compare different manufacturers for a specific month:

<CodeGroup>

```javascript JavaScript
async function compareManufacturers(month, makes) {
  const comparisons = await Promise.all(
    makes.map(async (make) => {
      const response = await fetch(`https://api.sgcarstrends.com/v1/cars/makes/${make}?month=${month}`, {
        headers: { 'Authorization': 'Bearer YOUR_API_KEY' }
      });
      const data = await response.json();
      return {
        make,
        total: data.data.reduce((sum, item) => sum + item.number, 0)
      };
    })
  );
  
  return comparisons.sort((a, b) => b.total - a.total);
}

const comparison = await compareManufacturers('2024-01', ['Toyota', 'Honda', 'BMW']);
```

</CodeGroup>

## Best Practices

### 1. URL Encoding

Always URL encode parameters with special characters:

<CodeGroup>

```javascript JavaScript
// Correct way to handle spaces and special characters
const vehicleType = 'Passenger Cars';
const encodedType = encodeURIComponent(vehicleType);
const url = `https://api.sgcarstrends.com/v1/cars/vehicle-types/${encodedType}`;
```

```python Python
import urllib.parse

vehicle_type = 'Passenger Cars'
encoded_type = urllib.parse.quote(vehicle_type)
url = f'https://api.sgcarstrends.com/v1/cars/vehicle-types/{encoded_type}'
```

</CodeGroup>

### 2. Parameter Validation

Validate parameters before making requests:

<CodeGroup>

```javascript JavaScript
function validateMonth(month) {
  const monthPattern = /^\d{4}-\d{2}$/;
  return monthPattern.test(month);
}

function validateFuelType(fuelType) {
  const validTypes = ['Petrol', 'Diesel', 'Electric', 'Hybrid', 'Others'];
  return validTypes.includes(fuelType);
}

// Usage
const month = '2024-01';
const fuelType = 'Electric';

if (validateMonth(month) && validateFuelType(fuelType)) {
  // Make API request
}
```

</CodeGroup>

### 3. Error Handling

Handle filtering errors gracefully:

<CodeGroup>

```javascript JavaScript
async function safeApiCall(url, params = {}) {
  try {
    const queryString = new URLSearchParams(params).toString();
    const fullUrl = queryString ? `${url}?${queryString}` : url;
    
    const response = await fetch(fullUrl, {
      headers: { 'Authorization': 'Bearer YOUR_API_KEY' }
    });
    
    if (!response.ok) {
      throw new Error(`HTTP error! status: ${response.status}`);
    }
    
    const data = await response.json();
    
    if (!data.success) {
      throw new Error(data.error || 'API request failed');
    }
    
    return data;
  } catch (error) {
    console.error('API call failed:', error.message);
    throw error;
  }
}

// Usage
try {
  const data = await safeApiCall('https://api.sgcarstrends.com/v1/cars', {
    month: '2024-01',
    fuel_type: 'Electric'
  });
  console.log(data);
} catch (error) {
  console.error('Failed to fetch data:', error.message);
}
```

</CodeGroup>

## Next Steps

<CardGroup cols={2}>
  <Card
    title="Comparison Analytics"
    icon="chart-bar"
    href="/guides/comparison-analytics"
  >
    Learn to compare data across time periods
  </Card>
  <Card
    title="API Reference"
    icon="book"
    href="/api-reference/overview"
  >
    Explore all available endpoints
  </Card>
  <Card
    title="Examples"
    icon="code"
    href="/examples/javascript"
  >
    See practical filtering examples
  </Card>
  <Card
    title="Data Models"
    icon="database"
    href="/guides/data-models"
  >
    Understand the data structure
  </Card>
</CardGroup>